# 2026-01-14 工作日誌

---

### 11:30 - 建立藍圖協作架構

- **要求**：重新設計 Agent 協作方式，改用藍圖模式取代傳統分支 + Merge
- **結果**：完成新協作架構實作
- **檔案**：
  - 新增 `.agent-workspace/blueprints/` 藍圖存放區
  - 新增 `.agent-workspace/working/` 副本工作區
  - 新增 `/blueprint`、`/claim`、`/integrate` 三個 slash command
  - 更新 Agent 配置檔（加入藍圖協作模式、類別標籤）
  - 更新 Publish Profile（排除工作區）
  - 更新 skills/INDEX.md（加入協作流程說明）

**新協作流程**：
1. `/blueprint` - Manager 分析任務、建立藍圖、分配 Agent
2. `/claim {agent-id} {task-id}` - Agent 領取任務、自動初始化
3. 各自開發（有衝突時在副本上修改）
4. Commit（副本 + 主代碼一起）
5. `/integrate {task-id}` - Super Agent 智能整合

**設計理念**：
- 用 AI 智能整合取代 Git Merge
- 減少 pull/push 同步負擔
- 透過副本避免同時修改同一檔案的競爭問題

---

### 16:00 - 統一請購單權限控制與 UI 顯示邏輯

- **要求**：請購單的審核按鈕和錯誤顯示邏輯與費用申請單不一致
- **結果**：完成權限控制和訊息顯示統一化
- **檔案**：`MgmSP/PurchaseRequestForm.aspx.vb`
- **修正內容**：
  1. 變數命名：`canApprove` → `isPuUser`（與費用申請單 `isApUser` 對應）
  2. 審核按鈕可見性：`btnApprove.Visible = isPuUser`（無權限者完全隱藏）
  3. 審核意見欄位：`txtApprovalComments.ReadOnly = Not isPuUser`
  4. 訊息顯示：統一使用 `ShowError`/`ShowSuccess`/`ShowWarning` 方法

**權限對照**：
| 單據類型 | 權限欄位 | 變數名稱 |
|----------|----------|----------|
| 費用報支 | AP_App | isApUser |
| 請購單 | PU_App | isPuUser |

---

### 14:30 - 修正 SAP 請購單 DI API 欄位映射

- **要求**：根據用戶審查回饋修正 CreatePurchaseRequestInSAP 方法
- **結果**：完成欄位映射修正
- **檔案**：`MgmSP/PurchaseRequestForm.aspx.vb`
- **修正內容**：
  1. `oPR.RequesterEmail` → `oPR.Requester`（員工代碼）
  2. 新增 `oPR.ReqType = 12`（請購類型：員工）
  3. 移除 `oPR.Lines.ItemDescription`，改寫入 `U_LineText` UDF
  4. 稅碼轉換：1→J1(應稅)、2→J0(零稅)、3→JX(免稅)

---

### 15:00 - 修正單據查詢權限檢查

- **要求**：採購人員設定 PU_App 後無法搜尋請購單
- **結果**：修正權限檢查邏輯，根據單據類型檢查對應權限
- **檔案**：`MgmSP/DocumentSearch.aspx.vb`
- **問題根因**：`CheckIsApUser()` 只檢查 `AP_App`，沒有檢查 `PU_App`
- **修正內容**：
  1. 新增 `isPuUser` 變數
  2. 新增 `CheckUserPermissions()` 同時讀取 AP_App 和 PU_App
  3. 新增 `HasViewAllPermission()` 根據單據類型返回對應權限
  4. 修改所有權限判斷邏輯使用新函數

**權限對照**：
| 單據類型 | 檢查欄位 |
|----------|----------|
| 費用報支 | AP_App |
| 請購單 | PU_App |

---

### 10:00 - CR 報告 SQL 查詢修正

- **要求**：修正 CR 報告中的明細重複問題
- **結果**：識別出 OCPR、CRD1 的多對一 JOIN 造成笛卡爾積
- **解決方案**：
  - jOPCH：直接使用 `H.Address`、`BP.CntctPrsn`，移除 OCPR/CRD1 JOIN
  - jOPRQ：使用 `OUTER APPLY (SELECT TOP 1 ...)` 強制只取一筆地址

---

### 17:15 - 統一 jOPRQ 狀態欄位格式（與 jOPCH 對齊）

- **要求**：jOPRQ 和 jOPCH 的狀態欄位設計不一致，需要統一
- **結果**：完成程式碼修改，產出資料庫遷移腳本
- **檔案**：
  - `MgmSP/PurchaseRequestForm.aspx.vb` - 更新狀態值判斷邏輯
  - `MgmSP/DocumentSearch.aspx.vb` - 更新查詢條件
  - `MgmSP/docs/DB_Migration_jOPRQ_Status_Unification.sql` - 新增遷移腳本
- **變更內容**：
  1. ApprovalStatus 值格式統一：`Pending`→`W`、`Approved`→`A`、`Rejected`→`R`
  2. 新增 SAP 整合欄位：B1PostStatus、B1PostDate、B1ErrMsg、DocEntry、DocNum
- **待辦**：用戶需在 SSMS 對 jtdb 執行遷移腳本

---

### 18:00 - 執行 /reflect 反思機制

- **要求**：執行定期反思，分析最近 7 天工作紀錄
- **結果**：
  - 統計 26 個任務，100% 成功率
  - P009 確認為常見模式（出現 2 次）
  - 搜尋 ASP.NET Web Forms 現代化和 Multi-Agent LLM 最新資訊
  - 產出完整反思報告
- **檔案**：
  - `work-logs/insights/reflect-2026-01-14.md` - 完整報告
  - `work-logs/TODO.md` - 新增 3 項待辦

---

### 18:30 - 修正 jOPRQ 狀態值修改遺漏

- **要求**：審查剛完成的 commit，確認是否有遺漏或重複過去的錯誤
- **結果**：發現並修正遺漏的狀態值比對
- **檔案**：`MgmSP/PurchaseRequestForm.aspx.vb`
- **問題**：第 1619-1621 行的按鈕可見性邏輯仍使用舊狀態值
  ```vb
  ' 修正前（錯誤）
  btnDelete.Visible = (approvalStatus = "Pending" OrElse approvalStatus = "Rejected")

  ' 修正後（正確）
  btnDelete.Visible = (approvalStatus = "W" OrElse approvalStatus = "R")
  ```
- **影響**：若未修正，待審核/已退回狀態的單據按鈕會永遠隱藏
- **教訓**：修改狀態值時，必須全域搜尋所有使用該值的位置

---

### 19:00 - 建立自動化測試基礎設施

- **要求**：建立 SQL 驗證腳本和 Playwright E2E 測試
- **結果**：完成兩套測試工具
- **檔案**：
  - `tests/sql/purchase-request-validation.sql` - SQL 資料驗證（10 項檢查）
  - `tests/e2e/` - Playwright 專案結構
    - `package.json` - 依賴配置
    - `playwright.config.ts` - 測試設定
    - `pages/login.page.ts` - 登入頁面物件
    - `pages/purchase-request.page.ts` - 請購單頁面物件
    - `tests/purchase-request.spec.ts` - 測試案例
    - `README.md` - 使用說明

**SQL 驗證項目**：
1. 孤兒明細檢查
2. 空單據檢查
3. 金額一致性（表頭 vs 明細）
4. 稅額一致性
5. 必填欄位檢查
6. 審核狀態一致性
7. SAP 過帳狀態
8. 明細計算驗證
9. 附件完整性
10. 統計摘要

**E2E 測試案例**：
- 基本 CRUD 操作
- 金額計算驗證
- 驗證規則測試
- 審核流程
- PDF 匯出
- 冒煙測試
