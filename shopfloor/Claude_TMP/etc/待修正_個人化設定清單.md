# 待修正：個人化設定清單

**日期**: 2025-11-14
**說明**: 以下是需要調整為通用版本的個人化設定，請直接在本檔案中編輯修正

---

## 1. SESSION_INIT.md 中的專案特定資訊

### 問題：專案名稱與階段

**當前內容**:

```
### 專案名稱
SapB1Solution - 費用申請單功能開發

### 當前階段
5 天衝刺計畫（2025-10-29 至 2025-11-02）
- 目標：完成費用申請單 Create 模式
- 架構：B1Transaction v1.1 規格
- 功能：AP Invoice + MDR 發票明細
```

**建議調整為**:

- [x] 改為範例專案或讓使用者自行填寫
- [x] 提供註解說明這裡應該填什麼

**您的修正方案**:

```
不要保留任何我舊專案的資訊
```

---

## 2. SESSION_INIT.md 中的技術環境

### 問題：VB.NET + SAP B1 專屬

**當前內容**:

```
### 技術環境
- **語言**：VB.NET WebForms (.NET Framework 4.0)
- **IDE**：Visual Studio（使用者端）
- **資料庫**：
  - 本機：jtdb (.\SQLEXPRESS2008R2)
  - SAP：JTTST1 (192.168.1.31)
- **API**：SAP Business One DI API
- **套件**：Newtonsoft.Json、iTextSharp、ExcelDataReader
```

**建議調整為**:

- [x] 改為通用的技術環境範例
- [ ] 提供多種語言/框架的範例
- [x] 註明這是需要替換的區塊

**您的修正方案**:

```
[請在此填寫您認為應該如何調整]
請提供 Python 範例，不要保留任何我舊專案的資訊。

```

---

## 3. SESSION_INIT.md 中的專案結構

### 問題：SAP B1 專案特定結構

**當前內容**:

```
### 專案結構
MgmSP/                    # 主要專案目錄
├── commcode/             # 共用程式碼
│   ├── CommUtil.vb       # 工具類別（SQL、DI API）
│   └── CommSignOff.vb    # 簽核相關
├── usermgm/              # 使用者管理
├── signoff/              # 簽核功能
└── [其他模組]/
```

**建議調整為**:

- [ ] 改為通用的專案結構範例
- [ ] 提供不同專案類型的範例（Web、API、Desktop 等）
- [ ] 註明這是範例，應根據實際專案調整

**您的修正方案**:

```
讓使用者決定他需要什麼樣的專案/框架架構，開始協作之後再與使用者討論產生
```

---

## 4. MCP Server 配置（SQL Server 2005）

### 問題：SQL Server 版本過舊

**當前內容**:

- 文件中多處提到 SQL Server 2005
- `.mcp.json` 中的路徑是個人環境

**建議調整為**:

- [ ] 支援較新的 SQL Server 版本（2012+）
- [ ] 提供通用的配置範例
- [ ] 註明 SQL Server 2005 的限制

**您的修正方案**:

```
[請在此填寫] 請改為較為通用的Sql 範例，不要保留2005的風格與資訊

```

---

ㄑㄧ##ㄥ. 溝通語言與術語

### 問題：繁體中文專屬術語

**當前內容**:

```
### 溝通語言與術語
- **語言**：繁體中文（Traditional Chinese）
- **Row** = 列（lié）
- **Column** = 欄（lán）
- **時區**：台灣 UTC+8
```

**建議調整為**:

- [ ] 改為語言中立的版本
- [ ] 提供多語言範例
- [x] 保留繁中但註明為範例

**您的修正方案**:

```
[請在此填寫]
您希望這個協作包支援哪些語言？
- 提供範本讓使用者自行調整？ 
```

---

## 6. shopfloor 目錄結構

### 問題：dNet 目錄名稱是 VB.NET 專屬

**當前內容**:

```
shopfloor/Claude_TMP/
├── SqlQuery/       - SQL 腳本
├── dNet/           - VB.NET 完整檔案
└── etc/            - 其他檔案
```

**建議調整為**:

- [x] 改為通用的子目錄名稱（如 `code/`）
- [ ] 提供多語言的範例結構
- [x] 讓使用者自訂子目錄名稱

**您的修正方案**:

```
[請在此填寫]
建議的通用目錄名稱？
- code/ 或 src/?
預設為code,預設目錄都用英文，讓使用者可以根據需求修改
```

---

## 7. 檔案命名規範

### 問題：VB.NET 專屬副檔名

**當前內容**:

```
2. VB.NET 完整檔案：直接使用類別名稱.vb 或頁面名稱.aspx
   範例：UserVendorTaxId.vb, ExpenseClaimForm.aspx
```

**建議調整為**:

- [ ] 改為語言中立的命名規範
- [x] 提供多語言範例（.py, .js, .cs 等）
- [ ] 通用化副檔名部分

**您的修正方案**:

```
[請在此填寫]請用.py為範例
```

---

## 8. 協作模式中的回應時間標籤

### 問題：台灣時區專屬

**當前內容**:

```
### 協作模式
- **回應結尾**：加上台灣時間標籤 `[YYYY-MM-DD HH:mm]`
```

**建議調整為**:

- [ ] 改為 UTC 或本地時間
- [x] 讓使用者自訂時區
- [x] 提供時區設定說明

**您的修正方案**:

```
[請在此填寫]
建議使用什麼時區格式？
UTC
可選時區
```

---

## 9. MCP Server 路徑

### 問題：個人環境路徑

**當前內容** (.mcp.json):

```json
{
  "mcpServers": {
    "sapb1-sql": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/home/jason/projects/vbnet/SapB1Solution/mcp-sqlserver",
        ...
      ]
    }
  }
}
```

**建議調整為**:

- [x] 使用相對路徑
- [ ] 提供範例並註明需要調整
- [ ] 說明如何設定路徑

**您的修正方案**:

```
[請在此填寫]
建議使用：
- 相對路徑 "./mcp-sqlserver"?

```

---

## 10. SAP B1 特定術語與概念

### 問題：文件中多處提到 SAP B1 API

**當前內容**:

- CommUtil.vb（SQL、DI API）
- AP Invoice + MDR 發票明細
- B1Transaction v1.1 規格

**建議調整為**:

- [x] 移除 SAP B1 特定內容
- [x] 改為通用的業務邏輯範例
- [ ] 保留但標註為「特定專案範例」

**您的修正方案**:

```
[請在此填寫]
如何處理這些 SAP B1 特定內容？
- 提供其他框架的範例（如 Django ORM、Entity Framework 等）?
提供Python FastAPI 範例
```

---

## 完成確認

請在完成所有問題回答後，在此打勾：

- [x] 我已完成所有問題的回答
- [x] 我確認修正方案適合分享給其他開發者

完成後請告訴我「已更新」，我會根據您的回答調整協作包的內容。

---

**補充說明區**：
如果有其他發現的個人化設定，請在此補充：

```
我的朋友主要使用Python，請以Python為核心重新整理優化規範與使用說明與範例
```
