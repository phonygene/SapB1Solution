# UI-UX 開發檢查清單

> UI-UX Agent 專用

---

## ASPX 頁面

- [ ] 控制項 ID 維持不變
- [ ] Layout 相對位置未改變
- [ ] JavaScript 事件處理正常
- [ ] 新增控制項有更新 designer.vb
- [ ] PostBack 後下拉選單有重新綁定

## CSS 樣式

- [ ] 使用 `jet-color-themes.css` 的變數
- [ ] 不硬編碼顏色值
- [ ] 圓角不超過 12px
- [ ] 陰影使用淡色調
- [ ] 按鈕有 hover 效果
- [ ] 輸入框有 focus 狀態

## 設計原則

- [ ] 文字與背景有足夠對比度
- [ ] 按鈕大小適合所在區塊
- [ ] 沒有使用高飽和的刺眼顏色
- [ ] 沒有使用外部 CSS 框架

## 響應式

- [ ] 考慮不同螢幕尺寸
- [ ] 表格在小螢幕有適當處理

## 影響範圍

- [ ] 考慮對其他頁面的影響
- [ ] 共用元件修改有全面測試

---

## 常見問題模式

### [P001] CSS 變數名稱不一致
- **症狀**：樣式套用失敗、主題切換無效
- **解法**：統一使用 jet-color-themes.css 定義的變數

### [P002] Master 樣式覆蓋子頁面
- **症狀**：子頁面 CSS 被覆蓋
- **解法**：調整載入順序，利用子頁面後載入的特性

---

## ModalPopupExtender 開發規範

> 多個彈窗容易發生事件混淆，必須嚴格遵守

### 必須設置
- [ ] 每個 ModalPopupExtender 必須有唯一的 `BehaviorID`
- [ ] 對應的 Panel 必須有 `style="display:none"`
- [ ] 每個彈窗內的控制項 ID 必須獨特（不同彈窗不能用同名 ID）

### 事件綁定
- [ ] 按鈕的 `OnClick` 事件必須明確指向對應的處理函數
- [ ] 處理函數中必須呼叫正確的彈窗 `.Show()` 方法
- [ ] 關閉按鈕的 `CancelControlID` 必須正確設置

### 開發流程
1. 參照現有實現（如 ExpenseClaimForm）
2. 複製代碼後**必須逐行檢查**所有 ID 和事件綁定
3. 開發完成後測試完整流程：開啟 → 輸入 → 搜尋 → 選取 → 關閉

---

## 從錯誤中學習（持續新增）

### 2026-01-08: 搜尋彈窗事件混淆
- **問題**: PurchaseRequestForm 品號搜尋按鈕觸發了供應商搜尋
- **根因**: 複製代碼時沒有仔細檢查事件綁定，ModalPopupExtender 沒有 BehaviorID
- **教訓**: 多個相似彈窗必須有獨特 BehaviorID，複製代碼後必須逐行檢查
