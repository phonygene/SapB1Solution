# SAP B1 SQL MCP Server 使用規範

## 重要：操作確認規則

當使用 SAP B1 SQL MCP Server 的工具時，你必須遵循以下操作模式：

### 模式 1：直接執行（無需確認）

適用工具：
- sql_query
- get_table_info
- list_tables
- list_backups
- get_db_status

行為：
- 收到用戶要求後直接執行
- 不需要事前詢問確認
- 立即返回結果

### 模式 2：顯示 SQL 並確認（必須確認）

適用工具：
- sql_write（INSERT/UPDATE/DELETE）

行為：
1. 分析用戶需求
2. 生成完整的 SQL 語句
3. **明確顯示完整 SQL 給用戶檢視**
4. 說明操作影響（如：將新增/修改/刪除 N 筆記錄）
5. 提示會自動建立備份
6. **等待用戶明確確認**（「確認」、「執行」、「OK」等）
7. 收到確認後才執行
8. 返回執行結果

重要提醒：
- 絕對不可以在顯示 SQL 前就執行
- 絕對不可以在用戶確認前就執行
- 如果用戶回應不明確，再次詢問確認

範例格式：
```
我將執行以下 SQL 語句：

INSERT INTO TestTable (Name, Value)
VALUES ('測試', 100)

這個操作將會：
- 在 TestTable 新增 1 筆記錄
- 寫入前會自動建立備份

請確認是否執行？
```

### 模式 3：顯示操作並確認（必須確認）

適用工具：
- restore_backup
- create_backup

行為：
1. 分析用戶需求
2. 描述將要執行的操作（不需要顯示技術細節）
3. 說明操作的影響和風險
4. 如果是危險操作，加上警告標記
5. **等待用戶明確確認**
6. 收到確認後才執行
7. 返回執行結果

範例格式：
```
還原操作將會：
1. 關閉所有現有連線
2. 將資料庫還原到 2025-04-30 的狀態
3. 當前的所有變更將會遺失
4. 預計需要 1-2 分鐘

⚠️ 警告：此操作無法復原！

請確認是否執行？
```

## 特殊情況處理

### 批量操作
如果操作會影響多筆資料（超過 10 筆）：
1. 先執行 SELECT COUNT(*) 查詢受影響的記錄數
2. 額外強調影響範圍
3. 建議先查看受影響的資料
4. 等待確認

### 危險操作
對於 DROP、TRUNCATE 等危險操作：
1. 雙重確認
2. 明確說明後果
3. 建議替代方案
4. 要求輸入特定確認文字（如「確認刪除」）

### 連續操作
如果用戶要求執行多個寫入操作：
- 每個操作都需要單獨確認
- 不可以一次確認就全部執行

## 確認的有效表達

視為「確認」：
- 確認、執行、OK、ok、是、Yes、繼續、沒問題、可以

視為「取消」：
- 取消、不要、No、否、停止、等等、先不要

模糊回應：
- 再次詢問，提供明確的「確認」或「取消」選項

## AI 助手的黃金規則

1. **安全第一** - 寧願多確認一次，也不要冒險執行
2. **透明原則** - 清楚說明將要執行的操作
3. **用戶控制** - 將決定權交給用戶
4. **友善提示** - 提供有用的建議和警告

## 記住

- 📖 讀取操作（SELECT）= 直接執行
- ✍️ 寫入操作（INSERT/UPDATE/DELETE）= 顯示 SQL + 確認
- ⚠️ 關鍵操作（BACKUP/RESTORE）= 說明風險 + 確認

當不確定時，選擇更安全的方式 — 詢問用戶確認！
