# Session Wrap - 階段性存檔（繼續工作）

階段性存檔，但**不結束對話**。

## 參數

- 無參數：正常模式，記錄所有工作內容
- `-s` 或 `--selective`：選擇性記錄模式，讓使用者選擇要排除的工作項目

## 執行步驟

### 正常模式（無參數）

1. 總結本次階段的工作成果
2. 建立歷史記錄檔案：
   - 路徑：`worklog/checkPoint_YYYYMMDD_HHMM.log`
   - 內容：詳細的工作記錄，包含時間戳記與完整細節
3. 更新最新狀態檔案：
   - 路徑：`worklog/LastCheckPoint.log`
   - 內容：最新的專案狀態快照、待辦清單、下次啟動建議
4. 向使用者提供彙整報告
5. **繼續工作**，不結束對話

### 選擇性記錄模式（`-s` 參數）

1. **分析並列出本次會話的所有工作項目**
   - 將工作內容分類（例如：功能開發、bug修復、配置調整、討論探索等）
   - 以簡短清單形式呈現（每項1-2行）
   - 標記每個項目的編號

2. **使用 AskUserQuestion 工具詢問使用者**
   - 問題：「以下是本次會話的工作項目，請選擇要排除不記錄的項目」
   - 選項：每個工作項目作為一個選項
   - 使用 `multiSelect: true` 允許多選

3. **根據使用者選擇生成記錄**
   - 排除使用者選擇的項目
   - 只記錄使用者保留的工作內容

4. **建立歷史記錄和更新狀態**（同正常模式，但僅包含保留的內容）

5. **向使用者報告**
   - 說明哪些內容已記錄
   - 哪些內容已排除
   - **繼續工作**，不結束對話

詳細的檔案格式請參考 `agent-os/standards/global/session-management.md`。
