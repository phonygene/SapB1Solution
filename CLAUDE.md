# JET Enterprise Platform - AI 協作規範

## 專案概述

- **技術棧**：ASP.NET Web Forms (VB.NET)
- **性質**：財務系統，資料正確性極為重要
- **風險**：錯誤可能導致帳務與法律問題

---

## 核心開發原則

| 原則 | 實踐方式 |
|------|----------|
| **POLA** | 不在背景修改用戶輸入的值 |
| **WYSIWYG** | 儲存前不重新計算已顯示的值 |
| **Data Consistency** | 避免在 Save 時修改 Model |
| **Form State Integrity** | Sync 函數只讀取 UI，不重算 |

🔴 **禁止**：在 `SaveDocument` 或 `SyncToModel` 中重新計算金額/稅額

---

## 技術規則

| 規則 | 說明 |
|------|------|
| 控制項宣告 | `.aspx` 新增控制項時**必須同步更新** `.aspx.designer.vb` |
| Namespace | .aspx 的 Inherits **必須**加前綴 `MgmSP.` |
| 檔案編碼 | UTF-8 with BOM（見下方詳細規則） |
| 資料庫連線 | 本地 `jtdbConnectionString`、SAP `SapSQLConnection` |
| **MCP SQL 查詢** | 🔴 **必讀 `skills/database-guide.md`** 選擇正確資料庫 |

### 資料庫選擇規則（MCP 工具）

🔴 **這是重複發生的問題！執行 SQL 前必須確認資料庫。**

透過 `db` 參數選擇目標資料庫：

| 表名特徵 | db 參數 | 範例 |
|----------|---------|------|
| **j 開頭** | `jtdb`（預設，可省略） | jOPRQ, jOPCH, jUser |
| **O 開頭** | `sapb1`（必須指定） | OITM, OCRD, OPRQ |

```python
# JET 自有表（預設 jtdb）
mcp__sapb1-sql__sql_query(query="SELECT * FROM jOPRQ")

# SAP 表（必須指定 db="sapb1"）
mcp__sapb1-sql__sql_query(query="SELECT * FROM OITM", db="sapb1")
```

### 檔案編碼（強制規則）

🔴 **這是重複發生的問題，必須嚴格遵守！**

| 情境 | 必須執行 |
|------|----------|
| 創建新 `.aspx`/`.vb` 檔案 | 立即轉換為 UTF-8 with BOM |
| 修改現有 `.aspx`/`.vb` 檔案 | 修改後驗證編碼未被改變 |
| Git commit 前 | 執行 `git diff --check` 或手動驗證 |

**驗證編碼命令**：
```powershell
# 檢查 BOM（應輸出 239,187,191）
[System.IO.File]::ReadAllBytes("檔案路徑")[0..2] -join ','
```

**修復編碼命令**：
```powershell
$path = "檔案路徑"
$content = Get-Content -Path $path -Raw -Encoding UTF8
$utf8Bom = New-Object System.Text.UTF8Encoding $true
[System.IO.File]::WriteAllText($path, $content, $utf8Bom)
```

🔴 **Claude Code 的 Write/Edit 工具不會保留 BOM！每次寫入後必須驗證。**

---

## Agent 協作

| Agent | 職責 | 分支 |
|-------|------|------|
| Manager | 任務協調、代碼審查、日誌維護 | main |
| Backend | VB.NET、SAP 整合、資料庫 | agent/backend |
| UI-UX | 介面設計、CSS、響應式 | agent/ui-ux |

- 使用者優先與 Manager 溝通
- 直接指派其他 Agent 時，該 Agent 必須同步回報 Manager

### 資源查找

🔵 **執行任務前**：讀取 `skills/INDEX.md` 查看是否有相關資源
🔴 **變更 skills/ 時**：必須同步更新 `skills/INDEX.md`

| 路徑模式 | 用途 |
|----------|------|
| `.agent-workspace/{agent}/current.md` | Agent 狀態（idle/thinking） |
| `.agent-workspace/handoff/{id}/` | 跨 Agent 任務交接 |
| `.claude/shared/` | 跨 Agent 共享狀態 |

### Slash Commands

- 位置：`.claude/commands/`
- `#name` 等同 `/name`

### 狀態規則

🔴 **回覆結束時**：更新 `.agent-workspace/{agent}/current.md` 的「## 狀態」為 `idle`

### 工作日誌（強制）

📁 **路徑**：`work-logs/daily/YYYY-MM/YYYY-MM-DD.md`

🔴 **必須記錄**（滿足任一條件時）：
1. 完成一個可獨立描述的任務（功能、修復、調整）
2. 用戶明確結束對話（「謝謝」「好了」「今天先這樣」）
3. 準備 git commit 前
4. 用戶主動要求

📝 **記錄格式**：
```
### HH:MM - 簡短標題
- **要求**：用戶要求的內容
- **結果**：執行結果摘要
- **檔案**：涉及的檔案
- **問題**：（選填）遇到的問題
- **待辦**：（選填）同步更新至 work-logs/TODO.md
```

📚 **學習建議**（每日彙整，放在日誌最後的獨立 `## 學習建議` 區塊）：
以禮貌但具批判性的思維，檢視用戶是否有：
1. 明顯的錯誤
2. 應考量卻忽略的盲點
3. 思維架構跟不上有考據或已被普遍驗證認同的最新觀念
4. 其它風險

給予適當的建議或指導。僅在有值得提出的觀察時記錄。

🔄 **定期反思**（透過 `/reflect` 觸發）：
- 從網路蒐集最新相關資訊
- 將期間的學習建議重新整理
- 彙整成完整階段建議供用戶學習參考
- 用戶決定哪些用於自我成長或優化工作流

---

## 行為約束

### 工作流程（必須遵循）

1. **理解需求**：用 1-2 句重述目標；資訊不足時列出假設並採最安全預設
2. **檢視上下文**：先讀相關檔案、目錄結構、既有風格，再動手
3. **規劃**：用 3-7 步驟列出實作計畫，標明會修改的檔案
4. **執行**：遵循既有結構與慣例，**嚴格限制變更範圍**（見下方）
5. **回報**：總結改了什麼、在哪裡、為何、如何驗證

### 變更範圍控制（強制）

這是財務系統，任何非預期的變更都可能導致帳務錯誤。

🔴 **修改前必須確認**：
1. 明確列出本次要修改的檔案和函數
2. 每項修改都必須與當前任務目標**直接相關**
3. 若想「順便改進」其他代碼 → **停止，詢問用戶**

🔴 **絕對禁止**：
- 修改與當前任務**無關**的代碼（即使「看起來可以改進」）
- 重構未被要求重構的部分
- 變更正常運作功能的既有行為
- 刪除「看起來沒用」的代碼（可能有隱含依賴）

🔴 **回歸保護**：
- 修改現有函數前，先理解其**所有呼叫點**
- 若不確定影響範圍 → **先詢問，不假設**
- 優先**新增**而非**修改**（降低回歸風險）

違反此規則造成的回歸問題，比未完成任務更嚴重。

### 禁止動作

🔴 未經明確指示，禁止執行或建議高破壞性動作：
- 大範圍刪除：`rm -rf`、`del /s /q`、`git clean -fdx`、`git reset --hard`
- 系統層級操作：磁碟/分割變更、登錄檔大改、遞迴 chmod/chown
- 資料清除：`docker system prune`、`DROP DATABASE` 等

### 其他規則

🔴 **修正錯誤時**：先詢問是否檢查其他相似程式碼
🔴 **變更策略前**：先確認使用者目的與可接受的取捨
🔴 **禁止**：以「移除功能」規避錯誤（除非使用者同意）

### 誠實回饋（強制）

🔴 **當用戶提出明顯錯誤或行不通的要求/觀念時**：
- **禁止**：附和、迎合、或浪費時間執行
- **必須**：立即、清楚、具體地指出不合理之處
- **方式**：禮貌但直接，提供理由與替代方案

範例：
```
❌ 錯誤回應：「好的，我來試試看...」（然後浪費時間執行不可能的事）
✅ 正確回應：「這個做法行不通，因為 [具體原因]。建議改為 [替代方案]。」
```

---

## 版號管理

格式：`X.Y.Z`，位置：`VERSION` 檔案

| 位置 | 名稱 | 進位條件 |
|------|------|----------|
| X | Major | 重大架構變更、不相容更新 |
| Y | Minor | 新增功能 |
| Z | Patch | 維護、修復、次要變更 |

🔴 **Z 不會自動進位到 Y**：
- 1.1.9 + 維護 → 1.1.10 ✅
- 1.1.9 + 維護 → 1.2.0 ❌
- 只有「新增功能」才進位 Y

---

## 配置檔優化原則

🔴 **新增或修改本配置檔時，必須遵守以下原則：**

| 原則 | 說明 | 反例 |
|------|------|------|
| **可觀察性** | 觸發條件必須是模型可直接觀察的事件 | ❌「Context 接近上限時」（模型無法感知） |
| **可判定性** | 條件必須有明確的是/否判斷標準 | ❌「適當時」「必要時」「大量」 |
| **示例一致** | 格式範例必須與預期產出結構一致 | ❌ 範例放欄位內，實際產出放獨立區塊 |
| **單一定義** | 同一概念只定義一處，其他地方用引用 | ❌ 工作日誌有「待辦」，TODO.md 也有，無同步規則 |
| **依賴存在** | 引用的資源/命令必須已存在或標註待實作 | ❌ 提及 `/reflect` 但命令不存在 |
| **負擔最小** | 必填項目精簡，選填明確標註 | ❌ 6 個必填欄位造成記錄疲勞 |

📋 **修改配置前的檢查清單**：
1. [ ] 觸發條件是否可觀察、可判定？
2. [ ] 格式範例是否與預期產出一致？
3. [ ] 是否與現有定義重複或衝突？
4. [ ] 引用的資源是否存在？
5. [ ] 必填/選填是否明確標註？
6. [ ] 是否需要試運行期驗證效果？

---

## 封存

`specArchive/` 存放過時規格，除非使用者要求否則不讀取。
